apiVersion: route.openshift.io/v1
kind: Route

metadata:
  labels:
    app: node
  name: node
  namespace: nathan-ns

spec:
  host: node-nathan-ns.apps-crc.testing
  port:
    targetPort: 8080
  to:
    kind: Service
    name: node